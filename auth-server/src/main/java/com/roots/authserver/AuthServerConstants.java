package com.roots.authserver;

/** Constants shared across the auth-server codebase. */
public final class AuthServerConstants {

    /** Role granted to every newly created account (signup and social login alike). */
    public static final String DEFAULT_ROLE = "member";

    private AuthServerConstants() {}
}
